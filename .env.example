# Placeholder-only local configuration template.
# Never commit real credentials, API keys, secrets, tokens, or signatures.

APP_NAME=replace-with-local-display-name
VERSION=0.0.0-local
ENVIRONMENT=development

# BingX VST readiness intentionally does not read this file.
# Its API key and secret are collected interactively through getpass.
